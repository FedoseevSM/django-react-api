import styles from "./CryptoCurrency.module.sass"

const renderSwitch = (value) => {
  switch (value) {
    case "btc":
      const qwe = "123"
      break
  }
}

export const CryptoCurrency = (value) => {
  renderSwitch(value)
  return qwe
}
